{
  "name": "sdf-kpi-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --env-file-if-exists=.env --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node tools/build-check.mjs",
    "reset": "node server.mjs --reset"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
